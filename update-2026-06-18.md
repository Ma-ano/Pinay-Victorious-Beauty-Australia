# Update 2026-06-18 
## Firebase Storage migration
- Profile photos now upload directly to Firebase Storage instead of local `public/images/`
- Removed `/api/upload` route and local filesystem saves
- Old profile photo is **deleted from Firebase Storage** when a new one is uploaded (prevents orphaned/duplicate images)
- Created `firebasestorage.rules` — users can only write to their own `profiles/{uid}/` path
- Added `remotePatterns` in `next.config.ts` for `firebasestorage.googleapis.com` so `next/image` can serve profile photos
# Later (4:10 PM +)
## Profile picture upload
- Added `photoURL` field to User type, stored in Firestore `users/{uid}.photoURL`
- Profile page now has clickable avatar with camera button — opens file picker, uploads via `/api/upload`, saves URL to Firestore
- Navbar avatar shows uploaded profile image; falls back to initial letter
- Register route, login, Google login all handle `photoURL`
- `updateProfile()` accepts `photoURL` for updating
## WhatsApp floating bubble
- Created `src/components/WhatsAppBubble.tsx` — fixed bottom-left green button with pulsing glow animation
- Shows auto-dismissing tooltip: "Deals & Offers — Reach out on WhatsApp" (5 seconds)
- Arrow points from tooltip into the WhatsApp icon
- Added `@keyframes whatsappPulse` animation in globals.css (green glow ring pulse 2.5s)
- Added to layout.tsx so it renders on every page
- `PromotionCards.tsx` now returns `null` (section replaced by persistent bubble)
## Footer updates
- Added WhatsApp link with icon in Support column
- Added Contact page link in Support column
- Added Pinterest social icon (4th icon) in Follow column
## Navbar fixes
- **Removed loading guard** — navbar now renders immediately alongside page content instead of showing empty placeholder while Firebase auth resolves
- **Fixed duplicate cart button** — removed second cart button that appeared below auth section after the cart-reorder change
- Cart position: `ThemeToggle → Cart → Auth (avatar/Login-Register) → ☰`
## Bug fixes batch 1
- Fixed dark mode autofill — added `:-webkit-autofill` CSS overrides in globals.css to prevent white background on browser dropdown/autofill
- Fixed OTP redirect + navbar realtime update — forced Firebase ID token refresh (`getIdToken(true)`) in `login()` so `emailVerified` is always read correctly
- Fixed React setState warning — moved `router.push("/login")` from render body to `useEffect` in ProfilePage.tsx
- Fixed navbar flash — added loading placeholder in CustomerNavbar that renders a silent skeleton until Firebase auth resolves, preventing the brief "logged out" state flicker
- Fixed "Each child in a list should have a unique key" error — loading guard was accidentally placed inside `navLinks.map()` callback instead of at component top level, causing map to return undefined for sibling links
## Navbar restructure
- Removed `py-3` from navbar container — navbar height now matches logo exactly (`h-14 md:h-20`)
- Spacer reduced from `h-32 md:h-48` to `h-14 md:h-20`
## Nav links
- Added standalone "Shop" link after "Home"
- Renamed dropdown "Shop" → "Categories" (now renders as Categories dropdown)
- Fixed duplicate key error: `key={link.href}` → `key={link.label}` (Shop & Categories shared `/shop` href)
- Nav order: `🔍 Home → Shop → Categories → Sale → About → Contact`
## Search — replaced persistent bar with floating dropdown
- Removed the persistent search bar row below nav links
- Added search icon (magnifying glass) as first item in nav links on desktop
- Clicking it opens a compact floating dropdown (`w-80`) anchored below the search button
- Dropdown contains input field + instant product results filtered by name/brand
- No more 3-second debounce — results appear immediately on each keystroke
- Animated slide-in/out with Framer Motion
## Search bubble closes when:
- Clicking outside the dropdown (click-outside handler)
- Pressing Escape
- Clicking a result or "View all results"
- Navbar auto-hides on scroll down
## Performance fixes
- Scroll handler throttled with `requestAnimationFrame` to reduce re-renders
- Removed unnecessary `resize` event listener from nav height measurement
- Removed `debouncing` state and loading animation (no longer needed with instant results)
## Firebase email verification
- Created `src/app/auth/verify/VerifyPage.tsx` — custom handler for email verification links
- Reads `oobCode` from URL, calls `applyActionCode()` to verify
- Fancy success/error UI with logo, animated checkmark, and "Continue to Login" button
- Created `src/app/auth/verify/page.tsx` thin wrapper
## Login page
- Shows green success banner when redirected from verification with `?verified=true`
- Uses `useSearchParams` to detect the query param
## Config needed in Firebase Console
- Authentication → Templates → Email verification → set Custom action URL to `https://<domain>/auth/verify`
- Customize email template HTML with brand logo and styling
- Set up Custom SMTP (Gmail) for branded sender